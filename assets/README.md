Add your local images here to use as assets for OffMap.

Place images and update the `PLACES` `img` paths in `app.py` to point to `assets/<filename>`.

Example:
- assets/kawah_ijen.jpg
- assets/islas_cies.jpg

Then in `app.py` set `place['img'] = 'assets/kawah_ijen.jpg'` for local usage.
